import { Lusitana, Montserrat } from 'next/font/google';

export const montserratt = Montserrat({ subsets: ['latin'] });

export const lusitana = Lusitana({
  weight: ['400', '700'],
  subsets: ['latin'],
});
